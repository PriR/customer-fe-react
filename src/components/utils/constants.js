const STATES = [
    {
        id: 1,
        name: "Valids"
    },
    {
        id: 0,
        name: "Invalids"
    }
]

const PATH = "http://localhost:8000";

const COUNTRIES = "/countries";

const CUSTOMERS = "/customers";

const SEARCH = "/search?"

export { STATES, PATH, COUNTRIES, CUSTOMERS, SEARCH }